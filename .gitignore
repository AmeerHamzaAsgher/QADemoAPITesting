# Dependencies
node_modules/

# Environment / secrets
.env
.env.local

# Logs
*.log
npm-debug.log*

# Newman / test reports (keep the directory itself, ignore generated report files)
reports/*
!reports/.gitkeep

# npm note: package-lock.json is intentionally NOT ignored below (kept for reproducible installs)

# OS files
.DS_Store
Thumbs.db

# Editor/IDE
.vscode/
.idea/

# Note:
# postman/collections/, postman/environments/, prompts/, and docs/ are
# intentionally NOT ignored — they are core, version-controlled project assets.

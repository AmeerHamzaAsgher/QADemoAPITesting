# Phase 5 Backup (pre-5.1)

These files are an exact, untouched snapshot of the Postman collection and environment as they stood at the end of **Phase 5 — API Data Chaining & Workflow Automation**, taken immediately before **Phase 5.1 — Postman UI Organization** made any changes.

```text
QA-Demo Project.postman_collection.backup.json      (SHA-256 identical to the live file at the start of Phase 5.1)
QA-Demo Environment.postman_environment.backup.json
```

Do not modify these files. They exist purely as a known-good reference point to diff against if Phase 5.1's presentation-only changes ever need to be verified or reverted. See `docs/POSTMAN_UI_ARCHITECTURE.md` for the before/after comparison performed using this backup.
